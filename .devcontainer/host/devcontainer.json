{
    "name": "Host PC",
    "image": "ghcr.io/jr-ros/jr_bot_host:amd64-v1.1.1",

    "workspaceMount": "source=${localWorkspaceFolder},target=/home/ubuntu/jr_bot_ws/src,type=bind",
    "workspaceFolder": "/home/ubuntu/jr_bot_ws",

    "privileged": true,

    "containerEnv": {
        "DISPLAY": "${localEnv:DISPLAY}"
    },

    "remoteUser": "ubuntu",

    "runArgs": [
        "--net=host",
        "--ipc=host",
        "--volume=/dev:/dev"
    ],
    
    "initializeCommand": "git submodule update --init --recursive && xhost +local:root",
    "postCreateCommand": "$WORKSPACE/src/config/scripts/post_create.sh"
}